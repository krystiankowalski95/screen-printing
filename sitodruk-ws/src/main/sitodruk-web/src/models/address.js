export default class Address {
    constructor(country, voivodeship, city, postalCode, street, streetNumber) {
      this.country = country;
      this.voivodeship = voivodeship;
      this.city = city;
      this.postalCode = postalCode;
      this.street = street;
      this.streetNumber = streetNumber;
    }
  }